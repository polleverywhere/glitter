# After you configure this file, deploy your app with `glitter push`
server {
  bucket_name       "app-distro-bucket"
  access_key        "access"
  secret_access_key "sekret"
}

app 'My Windows App' {
  description "The hotest app that you've ever seen on your PC."
  channel 'win-edge'
  channel 'win-beta'
  channel 'win'
}